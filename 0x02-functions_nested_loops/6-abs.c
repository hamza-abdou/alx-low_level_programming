#include <ctype.h>
#include <stdlib.h>
/**
 *_abs - camputes the obsolutes values
 *of in integer
 *@r: input number as an interger
 *Return: absolute value
 */
int _abs(int r)
{
        int nbre = abs(r);

        if (nbre)
        {
                return (nbre);
        }
        else
        {
                return (nbre);
        }
}
