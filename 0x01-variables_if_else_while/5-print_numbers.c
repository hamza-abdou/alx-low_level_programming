#include <stdio.h>
/**
 *main - Entry point
 *Description 'prints all single digit numbers of base 1'
 *Return: always 0
*/
int main(void)
{
        int n;

        for (n = 0; n <= 9; n++)
        {
                printf("%d \n", n);

        }
        return (0);
}
